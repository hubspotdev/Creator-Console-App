rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEFAULT RULE: Deny all direct client access
    // Cloud Functions use the Admin SDK, which bypasses these rules entirely.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}